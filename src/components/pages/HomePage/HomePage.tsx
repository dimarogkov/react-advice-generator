import { Card } from '../../elements/Card';
import { Decor } from '../../elements/Decor';
import { Btn, Label, Title } from '../../ui';

export const HomePage = () => {
    return (
        <Card>
            <Label>Label</Label>
            <Title>Title</Title>
            <Decor />
            <Btn />
        </Card>
    );
};
